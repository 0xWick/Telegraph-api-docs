# Epoch & Internal (X-Internal-Secret)

- **Paths:** 3 (`POST /internal/delivery`, `POST /internal/epoch-close`,
  `GET /internal/epoch/current`)
- **Auth:** `X-Internal-Secret` header on all endpoints.

See [overview](networks.md#epoch--internal-x-internal-secret) for full details.