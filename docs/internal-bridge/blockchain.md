# Blockchain (MPC wallet + signer + fees)

- **Paths:** 4 (`POST /` wallet create, `GET /signer`, `POST /signer`, `GET /fees`)
- **Auth:** `POST /` is rate-limited (100/min per IP); others none.

See [overview](networks.md#blockchain-mpc-wallet--signer--fees) for full details.